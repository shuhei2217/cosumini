# Claude Code local tooling (not part of the website)
.claude/

# Server-only secrets & runtime files (never commit / never deploy over)
config.php
orders.log
# アクセス集計データ（サーバー上でのみ生成・保持。デプロイで上書きしない）
data/access.json
